import { useEffect,useState } from 'react'; import { api } from '../api/client';
export default function MatchesPage(){const [matches,setMatches]=useState([]);useEffect(()=>{api.get('/matches').then(r=>setMatches(r.data))},[]);return <section><h1>Partidos</h1><div className="match-grid">{matches.map(m=><article className="match-card" key={m.id}><div className="match-meta"><span>#{m.id} · {m.fase}{m.grupo?` · Grupo ${m.grupo}`:''}</span><span className={`status ${m.estado}`}>{m.estado}</span></div><h3>{m.local} <b>{m.goles_local} - {m.goles_visitante}</b> {m.visitante}</h3><p>{m.fecha} · {m.hora_local} · {m.ciudad}</p></article>)}</div></section>}
